name: Container
on: push

jobs:
  node_docker:
    runs-on: ubuntu-latest
    container:
      image: 14-alpine
    steps:
      - name: Log node version
        run: |
          node -v
          cat /etc/os-release
